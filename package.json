{
  "private": true,
  "name": "safe-eval-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "start": "rm -rf dist && parcel build index.html --no-source-maps && hs -p 1234 dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1",
    "preact": "^8.4.2",
    "safe-eval": "^0.4.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.11.0"
  }
}
